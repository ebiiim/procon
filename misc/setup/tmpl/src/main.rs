#![allow(non_snake_case, unused_variables, unused_mut)]

use proconio::input;

fn main() {
    input! {
        N: i64,
        A: i64,
        B: i64,
        C: i64,
        mut X: [i64; N],
    }

    let mut ans = 0;

    println!("{:?}", ans)
}
